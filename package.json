{
  "name": "monorepo",
  "workspaces": [
    "demo",
    "react-ascii-text"
  ],
  "scripts": {
    "start:demo": "npm run dev -w demo",
    "build:demo": "npm run build -w demo",
    "start:package": "npm run dev -w react-ascii-text",
    "build:package": "npm run build -w react-ascii-text"
  },
  "private": true,
  "dependencies": {
    "resolve": "^1.22.4"
  },
  "devDependencies": {
    "tsup": "^8.0.2"
  }
}
