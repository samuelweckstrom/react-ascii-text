{
  "name": "monorepo",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm --filter react-ascii-text build",
    "build:demo": "pnpm --filter demo build",
    "dev": "pnpm --filter react-ascii-text dev",
    "dev:demo": "pnpm --filter demo dev"
  },
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
